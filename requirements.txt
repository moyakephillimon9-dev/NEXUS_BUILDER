# NEXUS Builder — Runtime Dependencies
# All core functionality uses Python stdlib only.
# Install with: pip install -r requirements.txt

# Optional: faster async event loop for Memory AI
# uvloop>=0.19.0

# Optional: linting (used in generated CI pipelines)
# flake8>=7.0.0

# Optional: security scanning (used in generated CI pipelines)
# bandit>=1.7.0
